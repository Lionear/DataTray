- **Plugin releases are published by the build pipeline.** Bumping a plugin's version now builds it,
  packages it and puts it in the Plugin Store on its own, so a new plugin version reaches the store the
  day it is finished instead of waiting for someone to build and upload it by hand. Store versions
  carry a build number (`0.8.0.41`) identifying the exact build a plugin came from.

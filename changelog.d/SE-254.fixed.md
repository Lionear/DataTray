- **The macOS application menu says "About DataTray" again.** It still carried Avalonia's own
  fallback item, so the first entry under the Apple logo read "About Avalonia" and opened the
  framework's about box instead of DataTray's.

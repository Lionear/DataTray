- **Star a query to keep it.** Every row in the History panel has a star; starred queries are kept in
  their own store, so they survive *Clear history* and don't fall out when the history rolls over. The
  star in the panel header switches the list to starred queries only, and a starred query whose history
  entry is long gone still opens in a query tab on double-click.

- **Preferences is easier to find your way around.** The eleven pages are grouped in the rail under
  Workspace, Querying, Access and Extensions instead of being one flat list, every page opens with its
  own title and a line saying what it is for, and the longer pages are split into numbered sections —
  so General now reads "1. Language & startup", "2. Window & instances", "3. Schema tree",
  "4. Updates" rather than a run of settings with the odd bold heading in between.
- **Preferences opens at the top of the page again.** The Updates channel cards used to drag the
  General page down with them as the window opened, so it started halfway through.

- **The query editor and result grid can now be resized** by dragging the splitter between them —
  previously they held a fixed 2:3 height ratio. The chosen height is remembered across restarts, the
  same way the resizable bottom tool panels (Output, Containers, AI activity) already are.

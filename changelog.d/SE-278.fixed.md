- **The macOS application menu actually says "About DataTray" now, and gained Preferences.**
  SE-254's earlier fix compiled but never took effect on screen: it set the menu after
  Avalonia.Native had already locked in its own fallback. Moving it earlier fixes that, and
  macOS also lost its Edit/Help menus — Preferences moved into the app menu (Cmd+,, standard
  macOS placement) and About was already there too, so both were left empty of anything else.
